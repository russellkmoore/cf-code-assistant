---
phase: 05-extract-shared-runtask-executor
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/__tests__/runtask.test.ts
autonomous: true
requirements: [BATCH-02]
must_haves:
  truths:
    - "A new runtask.test.ts asserts byte-identical buildPrompt output for ALL 11 AI-backed kinds with literal expected strings"
    - "explainCode resolve(input) is asserted across detailed/brief/eli5/default -> correct {tier, maxTokens}"
    - "transformCode validate passes at 8000 bytes and throws at 8001 bytes"
    - "runTask end-to-end against the mock AI returns {text, model, latency_ms} for a valid kind"
    - "The transformCode single-task over-cap path is explicitly asserted to return the byte-identical INPUT_TOO_LARGE envelope"
    - "The full suite (108 existing + new) is green and npx tsc --noEmit is clean"
  artifacts:
    - path: "src/__tests__/runtask.test.ts"
      provides: "BATCH-02 prompt-drift snapshot guard: per-kind buildPrompt byte-equality, explainCode resolve, transformCode cap, runTask smoke"
      contains: "TASK_SPECS"
  key_links:
    - from: "runtask.test.ts"
      to: "TASK_SPECS / runTask"
      via: "import from ../index"
      pattern: "from \"../index\""
    - from: "runtask.test.ts"
      to: "createMockEnv"
      via: "import from ./helpers for runTask end-to-end smoke"
      pattern: "createMockEnv"
---

<objective>
Add `src/__tests__/runtask.test.ts` (D-06 / BATCH-02): the byte-equality prompt-snapshot test that is the ONLY guard against prompt drift, since the existing AI mock (`createMockAI`) returns its canned response regardless of the prompt string and is structurally blind to whitespace/ordering/wording changes. The test asserts byte-identical `buildPrompt` output for all 11 AI-backed kinds with literal expected strings transcribed from RESEARCH.md's verbatim Per-Kind Extraction Table, asserts explainCode's depth resolver across all three depths plus default, asserts transformCode's validate at/over the 8KB boundary, and adds a runTask end-to-end smoke test plus an explicit transformCode over-cap envelope assertion.

Purpose: Close the one regression gap the AI-mocked 108-test suite cannot see. Literal expected strings are the contract — do not re-derive prompts from the code; transcribe from RESEARCH.md (the source of truth).
Output: New `src/__tests__/runtask.test.ts`; full suite (108 + new) green; tsc clean.
</objective>

<execution_context>
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-extract-shared-runtask-executor/05-RESEARCH.md
@.planning/phases/05-extract-shared-runtask-executor/05-PATTERNS.md

<interfaces>
<!-- Contracts exported by Plan 01 (Wave 1) that this test imports. Use directly. -->

From src/index.ts (added in Plan 01, test-export block):
  export { ..., runTask, TASK_SPECS };
  export type { ..., TaskKind, AIResult };

  TASK_SPECS.<kind>.buildPrompt(input) => string   // pure, byte-identical to current inline build
  TASK_SPECS.<kind>.resolve(input) => { tier: ModelTier, maxTokens: number }
  TASK_SPECS.transformCode.validate(input) => void  // throws ValidationError when code byteLength > 8000
  runTask(env, kind, input) => Promise<{text, model, latency_ms}>

From src/__tests__/helpers.ts:
  createMockEnv({ aiResponse?: string }) => Env   // AI.run returns { response } regardless of prompt — prompt-blind
  // createMockAI returns "mock-response" by default; pass aiResponse:"mock AI output" for smoke tests

Existing test idioms to reuse (PATTERNS.md lines 161-233):
  strOfLen(n) = "x".repeat(n)  // for the 8000/8001-byte boundary
  expect(() => ...).toThrow() / .not.toThrow()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write runtask.test.ts — prompt snapshots, explainCode resolve, transformCode cap, runTask smoke</name>
  <files>src/__tests__/runtask.test.ts</files>
  <read_first>
    - .planning/phases/05-extract-shared-runtask-executor/05-RESEARCH.md (lines 136-173 — Per-Kind Extraction Table + explainCode resolver = the verbatim expected strings to transcribe; lines 357-403 — example test surface; lines 175-197 — transformCode envelope)
    - .planning/phases/05-extract-shared-runtask-executor/05-PATTERNS.md (lines 161-233 — runtask.test.ts analogs: input-validation.test.ts schema-access, tool-handlers.test.ts createMockEnv usage, helpers.ts contract)
    - src/__tests__/helpers.ts (createMockEnv / createMockAI signatures)
    - src/__tests__/tool-handlers.test.ts (createMockEnv usage + getToolHandler pattern for the over-cap envelope assertion)
    - src/index.ts (the 11 TASK_SPECS entries as built in Plan 01, to confirm the input field names per kind)
  </read_first>
  <behavior>
    - buildPrompt byte-equality for ALL 11 kinds: generateCode, reviewCode, transformCode, scaffoldTests, quickTask, explainCode, generateDocs, generateTypes, fixBug, generateCommitMessage, generateWorkerBoilerplate — each `expect(TASK_SPECS.<kind>.buildPrompt(input)).toBe(<literal expected string transcribed from RESEARCH.md>)`. Cover the conditional branches: generateCode with and without optional language/style/context; generateWorkerBoilerplate with and without bindings; reviewCode with and without criteria (.filter(Boolean) drops the empty slot); quickTask returns instruction raw; generateCommitMessage uses the ```diff fence.
    - explainCode.resolve: detailed -> {tier:"standard", maxTokens:4096}; brief -> {tier:"fast", maxTokens:2048}; eli5 -> {tier:"fast", maxTokens:2048}; default (no depth) -> {tier:"fast", maxTokens:2048}. Plus byte-identical buildPrompt for each depth.
    - transformCode.validate: 8000 bytes (strOfLen(8000)) does NOT throw; 8001 bytes throws.
    - runTask smoke (createMockEnv aiResponse:"mock AI output"): runTask(env,"generateCode",{prompt:"hi"}) resolves text==="mock AI output", model==="@cf/qwen/qwen3-30b-a3b-fp8", typeof latency_ms==="number".
    - transformCode single-task over-cap (8001 bytes) through the handler: returns isError true with text starting "[ERROR: INPUT_TOO_LARGE] transformCode received 8001 bytes; cap is 8000." (byte-identical interpolation).
  </behavior>
  <action>
    Create `src/__tests__/runtask.test.ts` in the existing Workers pool layout (globals on, relative imports `../index` and `./helpers`; no new dev dependency, no `.snap` file — inline `toBe` per RESEARCH alternatives). Import `{ TASK_SPECS, runTask, createMcpServer }` and types `{ TaskKind }` from `../index`, and `{ createMockEnv }` from `./helpers`. Reuse the `strOfLen(n) = "x".repeat(n)` idiom for the byte boundary. Structure: (1) a `describe("BATCH-02: buildPrompt snapshots")` with one `it` per kind asserting `expect(TASK_SPECS.<kind>.buildPrompt(<input>)).toBe(<literal>)` — transcribe every expected string VERBATIM from RESEARCH.md lines 140-173; do NOT re-derive from code. Include the conditional-branch cases listed in <behavior>. (2) `describe("BATCH-02: explainCode resolve")` asserting all three depths + default -> {tier,maxTokens} via `toEqual`, plus per-depth buildPrompt byte-equality. (3) `describe("BATCH-02: transformCode 8KB cap")` asserting `validate({code: strOfLen(8000), instruction:"rename"})` does not throw and `strOfLen(8001)` throws (`.toThrow()` / `.not.toThrow()`). (4) `describe("BATCH-01: runTask wiring")` with the createMockEnv smoke test. (5) An explicit over-cap regression: invoke the transformCode handler (via createMcpServer's `_registeredTools.transformCode.handler`, mirroring tool-handlers.test.ts) with strOfLen(8001) and assert the returned envelope has `isError === true` and `content[0].text` begins with the exact `[ERROR: INPUT_TOO_LARGE] transformCode received 8001 bytes; cap is 8000.` prefix. Do NOT modify any existing test file or src/index.ts (Plan 01 owns src/index.ts). If a buildPrompt assertion fails, the literal expected string came from re-derivation rather than RESEARCH.md — fix the expected string to match RESEARCH.md, never loosen the assertion.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/__tests__/runtask.test.ts` exits 0
    - `npx vitest run src/__tests__/runtask.test.ts` exits 0
    - `npx vitest run` exits 0 (full suite: 108 existing + new runtask tests, all green)
    - `npx tsc --noEmit` exits 0
    - `grep -c "buildPrompt" src/__tests__/runtask.test.ts` returns >= 11 (every AI-backed kind asserted)
    - `grep -c "\.toBe(" src/__tests__/runtask.test.ts` returns >= 11 (literal byte-equality assertions present)
    - `grep -q "INPUT_TOO_LARGE" src/__tests__/runtask.test.ts` exits 0 (explicit over-cap envelope assertion present)
    - `grep -q "8001" src/__tests__/runtask.test.ts && grep -q "8000" src/__tests__/runtask.test.ts` exits 0 (boundary asserted)
    - `git diff --stat src/index.ts` shows no change from Plan 01 baseline (this plan owns only the test file)
  </acceptance_criteria>
  <done>runtask.test.ts exists asserting byte-identical buildPrompt for all 11 kinds, explainCode resolve across 3 depths + default, transformCode validate at 8000/8001, runTask smoke, and the over-cap INPUT_TOO_LARGE envelope; full suite (108 + new) green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a test file only. No runtime code, no entry point, no trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Prompt drift undetectable by AI-mocked suite | mitigate | This test IS the mitigation: byte-identical buildPrompt assertions per kind catch whitespace/ordering/wording drift the prompt-blind AI mock cannot see. Literal expected strings transcribed from RESEARCH.md, not re-derived from code, so the test independently pins the contract. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (reuses existing vitest + @cloudflare/vitest-pool-workers). No legitimacy checkpoint required. |

No new external attack surface. The security-relevant outcome is *coverage*: this test pins the prompt + tier/maxTokens contract so future edits cannot silently degrade output while the suite stays green.
</threat_model>

<verification>
- `npx vitest run` — full suite (108 existing + new runtask snapshots) green
- `npx tsc --noEmit` — clean
- `git diff --stat src/index.ts` — unchanged from Plan 01 (test file is the only new artifact)
</verification>

<success_criteria>
- runtask.test.ts asserts byte-identical buildPrompt output per kind for all 11 AI-backed kinds (the prompt-drift guard the AI-mocked suite cannot provide)
- Resolved tier/maxTokens asserted per kind, including explainCode across detailed/brief/eli5/default
- transformCode 8KB cap asserted (8000 passes, 8001 throws) and the single-task over-cap INPUT_TOO_LARGE envelope asserted byte-identical
- Full suite (108 + new) green and `npx tsc --noEmit` clean
</success_criteria>

<output>
Create `.planning/phases/05-extract-shared-runtask-executor/05-02-SUMMARY.md` when done
</output>

---
phase: 05-extract-shared-runtask-executor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
autonomous: true
requirements: [BATCH-01]
must_haves:
  truths:
    - "D-01: runTask(env, kind, input) is a full executor — looks up TASK_SPECS[kind], builds the prompt, calls runAIWithMetrics, returns {text, model, latency_ms}; the one source of truth for prompt + tier + maxTokens AND the AI call, for all 11 AI-backed kinds"
    - "D-02: All 11 AI-backed handler heads delegate to runTask; each handler's logToolInvocation/logToolError + makeToolError tail stays in place unchanged (routingInfo excluded)"
    - "D-03: Per-kind input validation is built into runTask now, reusing each tool's exact existing Zod caps (no weakening, no duplicate-with-drift)"
    - "D-04: Special-case logic travels with the kind in TASK_SPECS — explainCode resolves tier/maxTokens by depth (detailed -> standard/4096, brief|eli5|default -> fast/2048) and transformCode's 8KB byte cap fires before the AI call"
    - "D-05: runTask throws a typed ValidationError only for the transformCode cap; AI failures propagate untouched so the handler-tail msg === AI_TIMEOUT classification is byte-identical, and the over-cap single-task path returns the byte-identical INPUT_TOO_LARGE envelope logging error_type AI_ERROR"
    - "All 108 existing tests stay green with no assertion changes; npx tsc --noEmit is clean"
  artifacts:
    - path: "src/index.ts"
      provides: "TASK_SPECS map, runTask executor, ValidationError class, 11 delegating handlers, additive test exports"
      contains: "function runTask"
  key_links:
    - from: "runTask"
      to: "runAIWithMetrics"
      via: "single AI call with resolved tier/maxTokens"
      pattern: "runAIWithMetrics\\(env"
    - from: "11 handler heads"
      to: "runTask"
      via: "delegation"
      pattern: "runTask\\(env"
    - from: "TASK_SPECS.transformCode.validate"
      to: "ValidationError carrying codeBytes"
      via: "pre-AI byte cap throw caught by handler tail"
      pattern: "ValidationError"
---

<objective>
Extract the prompt-build + tier/maxTokens head of all 11 AI-backed tool handlers in `src/index.ts` into a single `TASK_SPECS` dispatch map plus a `runTask(env, kind, input)` full executor (D-01), then shrink each of the 11 handlers to delegate to `runTask` while keeping their per-tool logging + error-mapping tail in place (D-02). Per-kind input validation is built into `runTask` reusing each tool's exact existing Zod caps (D-03). explainCode's depth resolver and transformCode's 8KB cap travel into `TASK_SPECS` (D-04). runTask throws a typed `ValidationError` only for the transformCode cap; AI failures propagate untouched so the existing `msg === "AI_TIMEOUT"` tail classification stays byte-identical (D-05).

Purpose: Establish the single shared executor that both the single-task tools (now) and the future `code_assist_batch` tool (Phase 7) call, with observable behavior identical to today.
Output: Modified `src/index.ts` containing `TaskKind`, `TaskSpec`, `TASK_SPECS`, `ValidationError`, `runTask`, 11 delegating handlers, and additive test exports. All 108 existing tests stay green.
</objective>

<execution_context>
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-extract-shared-runtask-executor/05-CONTEXT.md
@.planning/phases/05-extract-shared-runtask-executor/05-RESEARCH.md
@.planning/phases/05-extract-shared-runtask-executor/05-PATTERNS.md

<interfaces>
<!-- Key existing contracts the executor wraps. Extracted from src/index.ts. Use directly. -->

src/index.ts:9    type ModelTier = "fast" | "standard";
src/index.ts:26   const AI_TIMEOUT_MS = 45_000;
src/index.ts:31   const TRANSFORM_CODE_MAX_BYTES = 8_000;
src/index.ts:168  interface AIResult { text: string; model: string; latency_ms: number; }  // EXPORTED test type — do NOT widen
src/index.ts:174  async function runAIWithMetrics(env: Env, tier: ModelTier, userPrompt: string, maxTokens = 4096): Promise<AIResult>
src/index.ts:191  function makeToolError(code: ErrorCode, toolName: string)
src/index.ts:205  function createMcpServer(env: Env)  // tools registered inside via server.registerTool(name, {description, inputSchema:<ZodRawShape>}, handler)
src/index.ts:757  export { resolveModel, isAllowedModel, timingSafeEqual, callModel, makeToolError, createMcpServer, authHandler, runAIWithMetrics, ALLOWED_MODELS, DEFAULT_MODELS };
src/index.ts:758  export type { ModelTier, ErrorCode, AIResult };

callModel rejects with `new Error("AI_TIMEOUT")` on its own 45s AbortController. runTask must NOT catch this — let it propagate so the tail `msg === "AI_TIMEOUT"` check is unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TASK_SPECS map, ValidationError, and runTask executor</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (full read; focus lines 9, 26, 31, 168-201, 205, 757-758 and the 11 handler heads at 222-560)
    - .planning/phases/05-extract-shared-runtask-executor/05-RESEARCH.md (lines 136-264 — Per-Kind Extraction Table, explainCode resolver, transformCode cap, TASK_SPECS type proposal: THE byte-level source of truth)
    - .planning/phases/05-extract-shared-runtask-executor/05-PATTERNS.md (lines 25-157 — analog lines, ValidationError "no analog" note at 278-286)
  </read_first>
  <action>
    Add above `createMcpServer` in src/index.ts (no new runtime dependency): (1) a `TaskKind` string-union of exactly the 11 AI-backed kinds — generateCode, reviewCode, transformCode, scaffoldTests, quickTask, explainCode, generateDocs, generateTypes, fixBug, generateCommitMessage, generateWorkerBoilerplate (routingInfo excluded). (2) A `TaskSpec` interface with `resolve(input) => {tier: ModelTier, maxTokens: number}`, `buildPrompt(input) => string`, and optional `validate?(input) => void`. (3) A `class ValidationError extends Error` carrying `codeBytes` (e.g. in a `meta` field) — its only consumer is the transformCode handler tail. (4) A `TASK_SPECS: Record<TaskKind, TaskSpec>` whose 11 entries are transcribed VERBATIM from RESEARCH.md lines 140-152 — do not re-derive or "improve" any string. Preserve every per-kind idiom exactly: generateCode and generateWorkerBoilerplate use the mutable `parts`-array-with-conditional-pushes then `parts.join("\n\n")`; reviewCode uses `.filter(Boolean).join("\n\n")`; quickTask's buildPrompt returns `input.instruction` raw with NO wrapping; the fenced blocks use bare three-backticks except generateCommitMessage which uses ```diff. Constant-tier resolves: generateCode/transformCode/scaffoldTests/generateDocs/generateTypes/fixBug/generateWorkerBoilerplate -> {standard, 8192} (reviewCode -> {standard, 4096}, quickTask -> {fast, 4096}, generateCommitMessage -> {fast, 1024}). explainCode.resolve branches on `depth` per D-04: detailed -> {standard, 4096}, brief|eli5|undefined -> {fast, 2048}; its buildPrompt uses `level = depth ?? "brief"` and the 3 depthInstructions strings verbatim from RESEARCH.md lines 162-168. transformCode.validate computes `new TextEncoder().encode(code).byteLength` and throws `ValidationError` (carrying codeBytes) when `> TRANSFORM_CODE_MAX_BYTES` (strict `>`, so 8000 passes / 8001 throws). Per D-03, each spec reuses the EXACT Zod caps already on that tool's inputSchema for any inner validation — do not weaken or duplicate-with-drift. Then add `async function runTask(env: Env, kind: TaskKind, input): Promise<AIResult>` that looks up `TASK_SPECS[kind]`, calls `spec.validate?.(input)`, resolves `{tier, maxTokens}` via `spec.resolve(input)`, and returns `runAIWithMetrics(env, tier, spec.buildPrompt(input), maxTokens)`. runTask does ZERO logging and does NOT catch AI errors (D-05). Finally, add `runTask`, `TASK_SPECS`, and the `TaskKind` type to the existing named test-export block at lines 757-758 (additive only). Do NOT widen the exported `AIResult` shape.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -c "function runTask" src/index.ts` returns >= 1
    - `grep -c "TASK_SPECS" src/index.ts` returns >= 2
    - `grep -c "class ValidationError" src/index.ts` returns 1
    - `grep -q "runTask, TASK_SPECS\|TASK_SPECS, runTask\|runTask," src/index.ts` (runTask present in export block) exits 0
    - `npx vitest run` exits 0 (the 108 existing tests still pass — TASK_SPECS/runTask are not yet wired into handlers, so behavior is unchanged)
  </acceptance_criteria>
  <done>TaskKind, TaskSpec, ValidationError, TASK_SPECS (11 verbatim entries), and runTask exist in src/index.ts; runTask/TASK_SPECS/TaskKind are exported; tsc clean; 108 tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate all 11 AI-backed handler heads to runTask</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (the 11 handlers at 222-560, the canonical catch tail at 233-238, transformCode envelope at 287-297, explainCode tier-log at 395)
    - .planning/phases/05-extract-shared-runtask-executor/05-RESEARCH.md (lines 199-228 — handler-tail error mapping + per-kind input_size_bytes list; lines 287 — explainCode tier-for-log flag)
    - .planning/phases/05-extract-shared-runtask-executor/05-PATTERNS.md (lines 100-153 — transformCode cap rebuild + shrunk-handler target shape)
  </read_first>
  <action>
    Replace each of the 11 AI-backed handler HEADS so the body becomes: `try { const result = await runTask(env, "<kind>", <validated input>); logToolInvocation({...}); return {content:[{type:"text", text: result.text}]}; } catch (err) { <unchanged tail> }`. Keep each handler's existing catch/tail BYTE-FOR-BYTE per D-02: the `msg === "AI_TIMEOUT" ? "AI_TIMEOUT" : "AI_ERROR"` classification, the per-tool `input_size_bytes` computation (verbatim per kind from RESEARCH.md lines 212-228 — generateCode uses `prompt + (context ?? "")`, reviewCode/scaffoldTests/explainCode/generateDocs/generateTypes use `code`, quickTask uses `instruction`, fixBug uses `code + error`, generateCommitMessage uses `diff`, generateWorkerBoilerplate uses `description`, transformCode uses the already-computed codeBytes), the `logToolError` call, and `makeToolError(errorType, "<name>")`. Do NOT move the tail into runTask (over-extraction anti-pattern). For explainCode: the tail's `logToolInvocation` tier must stay the resolved dynamic tier — re-derive it in the tail as `depth === "detailed" ? "standard" : "fast"` (do NOT widen AIResult). For transformCode: catch the `ValidationError` thrown by runTask and rebuild today's EXACT over-cap envelope from PATTERNS.md lines 100-112 — the `logToolError({ tool: "transformCode", error_type: "AI_ERROR", input_size_bytes: codeBytes })` call (keep the error_type:"AI_ERROR" quirk, do NOT "fix" it) and the byte-identical interpolated `[ERROR: INPUT_TOO_LARGE] ...` message (codeBytes, TRANSFORM_CODE_MAX_BYTES, AI_TIMEOUT_MS/1000 must all be reachable where the message is built — have the ValidationError carry codeBytes so the tail interpolates exactly as today), returning `{ content:[{type:"text" as const, text}], isError: true as const }`. AI failures must continue to propagate untouched from runAIWithMetrics so the tail classification is unchanged. routingInfo (static, no AI) is NOT touched. Keep all inputSchema ZodRawShapes as-is (do NOT refactor to z.object()).
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (all 108 existing tests pass with NO assertion changes — tool-handlers, observability, input-validation, error-sanitization green)
    - `npx tsc --noEmit` exits 0
    - `grep -c "runTask(env," src/index.ts` returns >= 11 (every AI-backed handler delegates)
    - `git diff --stat src/__tests__/` shows no modified existing test files (behavior preserved; assertions untouched)
    - `grep -c "error_type: \"AI_ERROR\"" src/index.ts` returns >= 1 (transformCode over-cap quirk preserved)
  </acceptance_criteria>
  <done>All 11 AI-backed handlers delegate to runTask with unchanged per-tool tails; transformCode over-cap returns the byte-identical INPUT_TOO_LARGE envelope; explainCode logs the resolved tier; 108 tests green; tsc clean; no existing test assertions changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> MCP tool (registerTool inputSchema) | Untrusted input crosses here. UNCHANGED this phase — the existing per-tool Zod caps + OAuth gate remain the boundary. |

This phase adds NO new external attack surface, request entry point, or trust boundary. It is an in-file refactor. The relevant security posture is *preservation*.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | transformCode 8KB byte cap | mitigate | Cap is a resource-abuse guard. Move only the byte-check into TASK_SPECS.transformCode.validate with strict `>` TRANSFORM_CODE_MAX_BYTES (8000 passes / 8001 throws); the over-cap path still fires BEFORE any AI call. runtask.test.ts (Plan 02) asserts the 8000/8001 boundary; tool-handlers.test.ts asserts the single-task envelope. |
| T-05-02 | Tampering | Per-kind input caps (D-03) | mitigate | Inner validation reuses each tool's EXACT existing Zod `.max()` caps — no cap weakened or duplicated-with-drift. input-validation.test.ts (unchanged) guards the boundary caps. |
| T-05-03 | Information Disclosure | runTask logging | mitigate | runTask does ZERO logging; all logToolInvocation/logToolError stay in the handler tail exactly as today, so no prompt/response/stack leaks into logs. observability.test.ts (unchanged) asserts no prompt/response/content/input/stack appears in any log — it turns red if runTask logs prompt text. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (zero new deps; verified in RESEARCH.md Package Legitimacy Audit). No legitimacy checkpoint required. |

No new high-severity threats to block on — this is a behavior-preserving internal refactor.
</threat_model>

<verification>
- `npx vitest run` — all 108 existing tests pass, no assertion changes
- `npx tsc --noEmit` — clean, no `as any` regressions in the new dispatch
- `git diff --stat src/__tests__/` — no existing test file modified
</verification>

<success_criteria>
- runTask + TASK_SPECS exist as the dispatch map (kind -> tier/maxTokens/buildPrompt + optional validate); all 11 AI-backed handler heads delegate to runTask; each handler's try/runAIWithMetrics-via-runTask/log/catch tail is unchanged (routingInfo excluded)
- All 108 existing tests pass and `npx tsc --noEmit` is clean — tool-handlers, observability, input-validation suites green with no assertion changes
- explainCode depth routing preserved (detailed -> standard/4096; brief/eli5/default -> fast/2048) as a function of input
- transformCode 8KB byte cap still fires pre-AI; over-cap single-task path returns the byte-identical INPUT_TOO_LARGE envelope and logs error_type AI_ERROR
</success_criteria>

<output>
Create `.planning/phases/05-extract-shared-runtask-executor/05-01-SUMMARY.md` when done
</output>

---
phase: 10-batch-per-task-cancellation-and-tier-override
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - CLAUDE.md
  - README.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [BATCH-F01, BATCH-F03]
must_haves:
  truths:
    - "CLAUDE.md Batch Tool section documents env.AI.run signal cancellation and per-task tier override"
    - "README.md batch fan-out note documents the per-task tier override"
    - "BATCH-F01 and BATCH-F03 are recorded as Validated in PROJECT.md and REQUIREMENTS.md traceability"
    - "STATE.md Deferred table shows F01/F03 resolved and F02 still deferred"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Batch Tool doc: signal cancellation + per-task tier override"
      contains: "tier"
    - path: "README.md"
      provides: "Batch fan-out note: per-task tier"
      contains: "tier"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability rows BATCH-F01/F03 → Validated"
      contains: "Validated"
  key_links:
    - from: ".planning/STATE.md Deferred Items table"
      to: "F01/F03 resolved, F02 remains"
      via: "table row status edits"
      pattern: "BATCH-F02"
---

<objective>
Update all documentation and tracking artifacts to reflect that BATCH-F01 and BATCH-F03 are now
implemented and validated (Plans 01 + 02). This closes the paperwork loop for Phase 10.

Purpose: Keep CLAUDE.md, README.md, and the .planning ledgers accurate so future phases and the
milestone retrospective have correct state. BATCH-F02 stays deferred.
Output: Edited CLAUDE.md, README.md, PROJECT.md, REQUIREMENTS.md, STATE.md.
</objective>

<execution_context>
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-batch-per-task-cancellation-and-tier-override/10-01-PLAN.md
@.planning/phases/10-batch-per-task-cancellation-and-tier-override/10-CONTEXT.md

<doc_anchors>
<!-- Exact edit targets anchored against LIVE source 2026-06-29. -->

CLAUDE.md:
- "## Batch Tool" section (env knobs: BATCH_CONCURRENCY/BATCH_MAX_TASKS/BATCH_TASK_TIMEOUT_MS, partial-results contract).
- "## Known Issues" section currently lists BATCH-F01/F02/F03 as "Deferred to a future milestone".
  After this phase, F01 and F03 are resolved — only F02 remains deferred.
- "## Model Tiers" table (fast=qwen3, standard=kimi-k2.5) — unchanged, but the per-task override note references it.

README.md:
- Line ~44: `code_assist_batch` tool table row.
- Line ~48: "**Batch fan-out:**" paragraph — add the per-task `tier` override note here.

.planning/REQUIREMENTS.md:
- "## Future Requirements" → "### Batch Enhancements" (lines 47-51): BATCH-F01, F02, F03 bullets.
  Mark F01 + F03 Resolved/Validated, keep F02 deferred.
- Traceability table (lines 86-87): change BATCH-F01/F03 status from "Planned (promoted from Future)" to "Validated — Phase 10".

.planning/PROJECT.md:
- "### Validated" list (ends ~line 55 with BATCH-10). Append BATCH-F01 and BATCH-F03 entries in the
  same style ("✓ **BATCH-F01**: ... — v2.0 Phase 10").

.planning/STATE.md:
- "## Deferred Items" table (lines 99-103): three rows F01/F02/F03. Update F01 + F03 status to
  "Resolved (Phase 10)"; leave F02 "Deferred". Update `last_activity` if appropriate.
</doc_anchors>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update product docs — CLAUDE.md + README.md</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>
    - CLAUDE.md (Batch Tool section, Known Issues section, Model Tiers table)
    - README.md:42-50 (tool table + Batch fan-out paragraph)
  </read_first>
  <action>
    In CLAUDE.md "## Batch Tool" section, add two notes: (1) a timed-out batch task now threads a real
    AbortSignal into env.AI.run so the subrequest is actually cancelled at the per-task deadline (not
    merely raced); (2) each batch task may carry an optional tier ("fast" or "standard") that overrides
    the kind's default tier for that task only (maxTokens stays the kind's value; the model is still
    governed by the tier-to-allowlist-to-KV abstraction — no raw model strings). In CLAUDE.md
    "## Known Issues", update the deferred list so BATCH-F01 and BATCH-F03 are removed or marked
    resolved and only BATCH-F02 (internal per-task retry) remains deferred. In README.md, extend the
    "Batch fan-out:" paragraph (~line 48) with a sentence: each task accepts an optional tier ("fast"
    or "standard") to override the kind's default model tier for that task, and a timed-out task is now
    actually cancelled (real AbortSignal). Do not alter the Model Tiers table values.
  </action>
  <verify>
    <automated>grep -qi "tier" CLAUDE.md && grep -qi "AbortSignal" CLAUDE.md && grep -qi "tier" README.md && grep -q "BATCH-F02" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Batch Tool section mentions both the env.AI.run signal cancellation and the per-task tier override.
    - CLAUDE.md Known Issues lists only BATCH-F02 as deferred (F01/F03 removed or marked resolved).
    - README.md Batch fan-out paragraph mentions the per-task tier override.
    - Model Tiers table values in CLAUDE.md are unchanged (fast=qwen3, standard=kimi-k2.5).
  </acceptance_criteria>
  <done>CLAUDE.md and README.md document the F01 cancellation and F03 tier override; only F02 remains listed as deferred.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update planning ledgers — PROJECT.md, REQUIREMENTS.md, STATE.md</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md:27-56 (Validated list)
    - .planning/REQUIREMENTS.md:43-95 (Future Requirements + Traceability table + reopening note)
    - .planning/STATE.md:95-110 (Deferred Items table + session continuity)
  </read_first>
  <action>
    In .planning/PROJECT.md "### Validated", append two entries after BATCH-10 in the existing style:
    a BATCH-F01 entry ("real AbortSignal threaded into env.AI.run — a timed-out batch task cancels its
    subrequest instead of best-effort racing; single-task handlers behavior-identical — v2.0 Phase 10")
    and a BATCH-F03 entry ("tier-only per-task override (fast or standard) in the batch input —
    overrides the model via the existing allowlist/KV abstraction, maxTokens preserved; no raw model
    strings at the MCP boundary — v2.0 Phase 10"). In .planning/REQUIREMENTS.md, update the
    "### Batch Enhancements" bullets so F01 and F03 are marked Resolved/Validated (Phase 10) while F02
    stays deferred, and change the Traceability table rows (lines 86-87) status to "Validated — Phase 10".
    In .planning/STATE.md "## Deferred Items" table, change the F01 and F03 rows' Status from "Deferred"
    to "Resolved (Phase 10)" and leave the F02 row as "Deferred"; update last_activity to note Phase 10
    docs complete.
  </action>
  <verify>
    <automated>grep -q "BATCH-F01" .planning/PROJECT.md && grep -q "BATCH-F03" .planning/PROJECT.md && grep -qE "Validated .* Phase 10|Resolved" .planning/REQUIREMENTS.md && grep -q "Resolved (Phase 10)" .planning/STATE.md && grep -q "BATCH-F02" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md Validated list contains a BATCH-F01 entry and a BATCH-F03 entry tagged "v2.0 Phase 10".
    - .planning/REQUIREMENTS.md Traceability rows for BATCH-F01 and BATCH-F03 read "Validated — Phase 10" (and F02 is unchanged/still deferred).
    - .planning/STATE.md Deferred Items table shows F01 and F03 as "Resolved (Phase 10)" and F02 as "Deferred".
    - No requirement other than F01/F03 is moved out of deferred status.
  </acceptance_criteria>
  <done>PROJECT.md, REQUIREMENTS.md, and STATE.md record F01/F03 as validated/resolved while F02 stays deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only plan; no code paths, no input handling, no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Repudiation / Information accuracy | planning ledgers (PROJECT/REQUIREMENTS/STATE) | mitigate | Status edits are scoped to F01/F03 only; F02 explicitly left deferred so the deferred-work record stays accurate for the next milestone. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs (docs only). No legitimacy gate required. |

No code, no new attack surface.
</threat_model>

<verification>
- All grep gates in the two tasks pass.
- No source or test file is modified by this plan (docs/ledgers only).
</verification>

<success_criteria>
- CLAUDE.md + README.md document the F01 signal cancellation and the F03 per-task tier override.
- PROJECT.md + REQUIREMENTS.md record BATCH-F01/F03 as Validated (Phase 10).
- STATE.md Deferred table shows F01/F03 resolved, F02 still deferred.
</success_criteria>

<output>
Create `.planning/phases/10-batch-per-task-cancellation-and-tier-override/10-03-SUMMARY.md` when done.
</output>

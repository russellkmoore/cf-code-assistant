---
phase: 10-batch-per-task-cancellation-and-tier-override
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/__tests__/tool-handlers.test.ts
  - src/__tests__/runtask.test.ts
  - src/__tests__/batch-tool.test.ts
autonomous: true
requirements: [BATCH-F01, BATCH-F03]
must_haves:
  truths:
    - "A test proves env.AI.run is invoked with a 3rd arg whose signal is an AbortSignal"
    - "A test proves a pre-aborted external signal aborts the AI call (signal-honoring mock)"
    - "A test proves runTask(env,'generateCode',input,{tier:'fast'}) resolves via the fast model"
    - "A test proves omitting tier uses the kind default, and a tier override does NOT change maxTokens"
    - "A test proves BatchTaskInputSchema accepts fast/standard and rejects other strings"
    - "A test proves a batch task {kind,tier:'fast',input} overrides through executeBatch + adapter"
    - "All 7 named regression suites stay green (no weakening)"
  artifacts:
    - path: "src/__tests__/tool-handlers.test.ts"
      provides: "F01 signal-threading + pre-aborted-signal tests"
      contains: "AbortSignal"
    - path: "src/__tests__/runtask.test.ts"
      provides: "F03 tier-override + maxTokens-preserved tests"
      contains: 'tier: "fast"'
    - path: "src/__tests__/batch-tool.test.ts"
      provides: "F03 schema accept/reject + adapter tier-flow tests"
      contains: "BatchTaskInputSchema"
  key_links:
    - from: "F01 test signal-honoring mock"
      to: "env.AI.run 3rd-arg signal"
      via: "custom vi.fn mock that inspects/honors AiOptions.signal"
      pattern: "instanceof AbortSignal"
    - from: "F03 tier-override test"
      to: "result.model"
      via: "assert result.model === DEFAULT_MODELS.fast"
      pattern: "DEFAULT_MODELS\\.(fast|standard)"
---

<objective>
Add the six Nyquist Wave-0 tests proving BATCH-F01 and BATCH-F03, and confirm the seven existing
regression suites stay green. The source changes (Plan 01) make these tests pass; this plan proves
they work and that single-task behavior is unchanged.

Purpose: The automated unit coverage is the gate (manual MCP-Inspector verification is non-gating per
10-VALIDATION.md). Every BATCH-F01/F03 row in the validation map lands on a task here.
Output: Extended test files; `npm test` green (142 existing + new cases).
</objective>

<execution_context>
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-batch-per-task-cancellation-and-tier-override/10-VALIDATION.md
@.planning/phases/10-batch-per-task-cancellation-and-tier-override/10-01-PLAN.md

<interfaces>
<!-- Test-harness facts anchored against LIVE source 2026-06-29. -->

Test helpers (src/__tests__/helpers.ts):
- `createMockEnv({ aiResponse?, kvData?, ... })` → Env. AI mock is `createMockAI` =
  `{ run: vi.fn(async () => ({ response })) }` — ARGUMENT-AGNOSTIC. It ignores all args, so:
  - For F01 signal assertions, you can spy `env.AI.run` (it's a vi.fn) and read `mock.calls[0][2]?.signal`.
  - For a signal-HONORING mock (pre-abort test), build a local `vi.fn` that reads the 3rd arg's
    `signal` and rejects/throws if `signal?.aborted` — do NOT use the shared argument-agnostic mock for that case.

Imports already used by the suites:
- runtask.test.ts: `import { TASK_SPECS, runTask, createMcpServer, DEFAULT_MODELS } from "../index";`
  - existing pattern asserts `result.model === DEFAULT_MODELS.standard` and `result.model === "@cf/qwen/qwen3-30b-a3b-fp8"` (the fast model). Reuse `DEFAULT_MODELS.fast` for the override assertion.
- batch-tool.test.ts: `import { BatchOutputSchema, deriveErrorType, createMcpServer } from "../index";`
  and `import type { BatchTask, BatchConfig, RunTask } from "../batch";`. For the schema accept/reject
  test, import `BatchTaskInputSchema` from "../index" (export it if not already exported — see note).
- tool-handlers.test.ts: uses `getToolHandler(env, name)` accessing `(server as any)._registeredTools`.

DEFAULT_MODELS (post-Phase-9): `fast` = `@cf/qwen/qwen3-30b-a3b-fp8`, `standard` = Kimi. generateCode's
kind default tier is `standard`, so a `tier:"fast"` override flips `result.model` to DEFAULT_MODELS.fast.
</interfaces>

<schema_export_note>
`BatchTaskInputSchema` may not currently be in the index.ts value-export list (line 1032). The F03
schema accept/reject test needs it. If `import { BatchTaskInputSchema } from "../index"` fails to
resolve, add `BatchTaskInputSchema` to the `export { ... }` list at src/index.ts:1032 (it is a
const value, so it goes in the value-export list, not the type-export list). This is a test-support
export, consistent with the existing `BatchOutputSchema` export already there.
</schema_export_note>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BATCH-F01 tests — signal threading + pre-aborted abort</name>
  <files>src/__tests__/tool-handlers.test.ts</files>
  <read_first>
    - src/__tests__/tool-handlers.test.ts:1-30 (imports, getToolHandler, beforeEach mock setup)
    - src/__tests__/helpers.ts (createMockEnv / createMockAI — argument-agnostic vi.fn)
    - src/index.ts:133-183 (callModel + runAIWithMetrics — what the tests exercise)
  </read_first>
  <behavior>
    - Test "signal": invoking any AI-backed path (e.g. via runTask or a handler) calls `env.AI.run` such that `env.AI.run.mock.calls[0][2]?.signal instanceof AbortSignal` is true.
    - Test "abort": a local signal-honoring AI mock that throws when its 3rd-arg `signal?.aborted` is true, driven by a pre-aborted external signal threaded through runTask's `opts.signal`, causes the call to reject/abort (proves the external signal reaches env.AI.run).
  </behavior>
  <action>
    Add a new `describe("BATCH-F01: AbortSignal threading", ...)` block to tool-handlers.test.ts. For
    the "signal" case: build an env via `createMockEnv`, invoke an AI-backed path (call `runTask`
    imported from "../index", or a fast-tier handler), then assert
    `(env.AI.run as any).mock.calls[0][2]?.signal instanceof AbortSignal`. For the "abort" case: build
    a custom env whose `AI.run` is a `vi.fn` that inspects its 3rd argument and, if `signal?.aborted`,
    throws (or rejects) `new Error("AI_ABORTED")`; create an `AbortController`, call `controller.abort()`
    BEFORE dispatch, then call `runTask(env, "quickTask", input, { signal: controller.signal })` and
    assert the promise rejects / the result envelope reflects the abort. Name the it() blocks so
    `-t "signal"` and `-t "abort"` select them. Do NOT weaken or modify existing tool-handlers tests.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/tool-handlers.test.ts -t "signal" && npx vitest run src/__tests__/tool-handlers.test.ts -t "abort" && npx vitest run src/__tests__/tool-handlers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/tool-handlers.test.ts -t "signal"` exits 0 and runs ≥1 test.
    - `npx vitest run src/__tests__/tool-handlers.test.ts -t "abort"` exits 0 and runs ≥1 test.
    - The "signal" test asserts `... instanceof AbortSignal` on the 3rd arg of an `env.AI.run` call.
    - The full `tool-handlers.test.ts` suite (17 existing + new) exits 0 — no existing assertion changed.
  </acceptance_criteria>
  <done>Two F01 tests prove the signal is threaded into env.AI.run and a pre-aborted signal aborts the call; existing handler tests stay green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BATCH-F03 tests — tier override + maxTokens preserved + schema + adapter flow</name>
  <files>src/__tests__/runtask.test.ts, src/__tests__/batch-tool.test.ts</files>
  <read_first>
    - src/__tests__/runtask.test.ts:1-14, 290-315 (imports, DEFAULT_MODELS usage, runTask wiring tests)
    - src/__tests__/batch-tool.test.ts:1-45 (imports, getToolHandler, enrich helper, stdCfg)
    - src/index.ts:389-416 (runTask opts + BatchTaskInputSchema — from Plan 01)
    - src/batch.ts:48-54, 119-150 (BatchTask.tier, executeBatch — adapter target)
  </read_first>
  <behavior>
    - runtask "tier": `runTask(env, "generateCode", input, { tier: "fast" })` → `result.model === DEFAULT_MODELS.fast`.
    - runtask default: `runTask(env, "generateCode", input)` (no opts) → `result.model === DEFAULT_MODELS.standard`.
    - runtask "maxTokens": a `tier:"fast"` override does NOT change the max_tokens passed to env.AI.run — assert `env.AI.run.mock.calls[0][1].max_tokens` equals the kind's default for generateCode (same value with and without the override).
    - batch-tool "schema": `BatchTaskInputSchema.safeParse({ kind:"generateCode", input:{}, tier:"fast" }).success === true`; same with `tier:"standard"`; `tier:"premium"` (or any other string) → `success === false`.
    - batch-tool "tier": a `BatchTask { kind:"generateCode", tier:"fast", input }` run through `executeBatch(tasks, cfg, adapter)` (with a real `runTask`-backed adapter mirroring runBatch) resolves via the fast model — assert the ok result's `model` is DEFAULT_MODELS.fast (or spy env.AI.run model arg = DEFAULT_MODELS.fast).
  </behavior>
  <action>
    In runtask.test.ts, add a `describe("BATCH-F03: per-task tier override", ...)` with it() blocks
    named to match `-t "tier"` and `-t "maxTokens"`. Use `createMockEnv({ aiResponse: ... })` and
    `import { DEFAULT_MODELS } from "../index"`. Assert override → `result.model === DEFAULT_MODELS.fast`,
    default → `result.model === DEFAULT_MODELS.standard`, and that `(env.AI.run as any).mock.calls[0][1].max_tokens`
    is identical with and without the override (proving maxTokens is the kind's, not tier-derived).
    In batch-tool.test.ts, add a `describe("BATCH-F03: tier override + schema", ...)`: import
    `BatchTaskInputSchema` from "../index" (export it if needed — see schema_export_note) and assert
    `.safeParse` accepts `tier:"fast"`/`"standard"` and rejects another string (name it for `-t "schema"`).
    Add an adapter-flow test (name it for `-t "tier"`) that builds an adapter
    `(batchTask, signal) => runTask(env, batchTask.kind, batchTask.input, { tier: batchTask.tier, signal })`
    (mirroring runBatch), runs `executeBatch([{ kind:"generateCode", tier:"fast", input:{prompt:"hi"} }], stdCfg, adapter)`,
    and asserts the ok entry's enriched/raw result reflects the fast model. Do NOT weaken existing tests.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/runtask.test.ts -t "tier" && npx vitest run src/__tests__/runtask.test.ts -t "maxTokens" && npx vitest run src/__tests__/batch-tool.test.ts -t "schema" && npx vitest run src/__tests__/batch-tool.test.ts -t "tier" && npx vitest run src/__tests__/runtask.test.ts src/__tests__/batch-tool.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/runtask.test.ts -t "tier"` exits 0 (override → DEFAULT_MODELS.fast; default → DEFAULT_MODELS.standard asserted).
    - `npx vitest run src/__tests__/runtask.test.ts -t "maxTokens"` exits 0 and asserts max_tokens unchanged by the tier override.
    - `npx vitest run src/__tests__/batch-tool.test.ts -t "schema"` exits 0 (accepts fast/standard, rejects other).
    - `npx vitest run src/__tests__/batch-tool.test.ts -t "tier"` exits 0 (batch task tier flows through executeBatch+adapter to the fast model).
    - Full runtask.test.ts and batch-tool.test.ts suites exit 0 — no existing assertion weakened.
  </acceptance_criteria>
  <done>Four F03 tests prove tier override resolves the fast model, preserves maxTokens, the schema accepts/rejects tiers, and the tier flows through the adapter; existing tests stay green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full-suite regression gate</name>
  <files>src/__tests__/runtask.test.ts</files>
  <read_first>
    - .planning/phases/10-batch-per-task-cancellation-and-tier-override/10-VALIDATION.md (regression-proof table — the 7 must-stay-green suites)
  </read_first>
  <action>
    Run the full test suite and the type gate to prove no regression across the seven named guards
    (runtask, tool-handlers, model-routing, batch, batch-tool, batch-e2e, observability). This task
    adds NO new code — it is the wave-completion gate. If any suite is red, fix the offending NEW test
    (never weaken an existing assertion to make it pass). The `<files>` entry is nominal (the gate may
    touch a new test only if a fix is required).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm test` exits 0 with all of these suites green: runtask.test.ts, tool-handlers.test.ts, model-routing.test.ts, batch.test.ts, batch-tool.test.ts, batch-e2e.test.ts, observability.test.ts.
    - Total it() count ≥ 142 + 6 new (no existing test deleted or skipped).
  </acceptance_criteria>
  <done>Full suite and type-check are green; all 7 regression suites pass; 6 new F01/F03 tests present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → runTask/env.AI.run | Tests assert the security-relevant signal threading + tier allowlist behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | regression of F01/F03 security behavior | mitigate | The schema accept/reject test (T-10-01 proof) and the signal-instanceof test (T-10-02 proof) are committed gates; the 7 regression suites prove single-task behavior is unchanged. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (vitest + Workers pool already present per 10-VALIDATION.md). No legitimacy gate required. |

No new external attack surface; tests are the gate for the mitigations claimed in Plan 01.
</threat_model>

<verification>
- All 6 Wave-0 tests pass via their `-t` selectors.
- `npm test` green across the 7 named regression suites + new cases.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- F01: env.AI.run 3rd-arg signal is an AbortSignal; pre-aborted signal aborts the call.
- F03: tier override resolves the fast model; maxTokens preserved; schema accept/reject; adapter tier flow.
- 7 regression suites green; no existing assertion weakened.
</success_criteria>

<output>
Create `.planning/phases/10-batch-per-task-cancellation-and-tier-override/10-02-SUMMARY.md` when done.
</output>

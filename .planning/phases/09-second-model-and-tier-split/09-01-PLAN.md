---
phase: 09-second-model-and-tier-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worker-configuration.d.ts
  - src/index.ts
  - src/__tests__/model-routing.test.ts
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [MODEL-03]

must_haves:
  truths:
    - "resolveModel(env, \"standard\") with empty KV resolves to the chosen Kimi id"
    - "resolveModel(env, \"fast\") with empty KV still resolves to @cf/qwen/qwen3-30b-a3b-fp8"
    - "isAllowedModel(<chosen Kimi id>) returns true; the Kimi id is a valid keyof AiModels (compile-time proven)"
    - "Tier names (\"fast\" / \"standard\") and the KV override keys (config:model:fast / config:model:standard) are unchanged"
    - "Every TASK_SPECS[kind].resolve() output is unchanged (no TASK_SPECS edits)"
    - "KV self-healing (delete-invalid-and-fall-back) still works for the standard tier"
    - "npx tsc --noEmit is clean and the full test suite is green (existing 162 + new cases)"
  artifacts:
    - path: "src/index.ts"
      provides: "ALLOWED_MODELS includes the chosen Kimi id; DEFAULT_MODELS.standard is the chosen Kimi id"
      contains: "moonshotai/kimi"
    - path: "src/__tests__/model-routing.test.ts"
      provides: "New cases asserting Kimi allowlist membership and standard-tier resolution"
      contains: "kimi"
    - path: "worker-configuration.d.ts"
      provides: "Regenerated AiModels type containing the chosen Kimi id as a key"
      contains: "kimi"
  key_links:
    - from: "src/index.ts DEFAULT_MODELS.standard"
      to: "ALLOWED_MODELS"
      via: "satisfies ReadonlyArray<keyof AiModels> + isAllowedModel allowlist membership"
      pattern: "ALLOWED_MODELS"
    - from: "src/index.ts ALLOWED_MODELS"
      to: "worker-configuration.d.ts AiModels"
      via: "as const satisfies ReadonlyArray<keyof AiModels>"
      pattern: "keyof AiModels"
---

<objective>
Make the dormant two-tier model routing real: point the `standard` tier at a Kimi
coding-optimized model while `fast` stays on qwen3-30b. This is a tightly-scoped,
behavior-shape-preserving change — only the model behind `standard` changes; tier
names, KV keys, and every per-kind resolve() output stay identical.

Purpose: Differentiate the two tiers so the abstraction buys something, and enable
Phase 10's per-task tier override (BATCH-F03) — meaningful only once the tiers
resolve to different models.

Output: Updated ALLOWED_MODELS + DEFAULT_MODELS in src/index.ts, regenerated
worker-configuration.d.ts, new model-routing test cases, and refreshed Model Tiers
docs in CLAUDE.md + README.md.

Design is LOCKED in 09-CONTEXT.md (sourced from the approved plan, Change 1). Do NOT
re-explore model choice or tier topology. The only in-plan decision is the gated
Kimi-id selection in Task 1, made by inspecting the regenerated types.
</objective>

<execution_context>
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-second-model-and-tier-split/09-CONTEXT.md

@src/index.ts
@src/__tests__/model-routing.test.ts
@src/__tests__/helpers.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from src/index.ts. Use directly — no exploration. -->

From src/index.ts (current state — the edit targets):
- `type ModelTier = "fast" | "standard";`
- `const ALLOWED_MODELS = ["@cf/qwen/qwen3-30b-a3b-fp8"] as const satisfies ReadonlyArray<keyof AiModels>;` (line 13)
- `function isAllowedModel(model: string): model is AllowedModel` — membership test over ALLOWED_MODELS (line 19)
- `const DEFAULT_MODELS: Record<ModelTier, keyof AiModels> = { fast: "@cf/qwen/qwen3-30b-a3b-fp8", standard: "@cf/qwen/qwen3-30b-a3b-fp8" };` (line 23)
- `async function resolveModel(env: Env, tier: ModelTier): Promise<keyof AiModels>` — reads `config:model:${tier}` from KV; returns override if isAllowedModel, else self-heals (deletes invalid key) and returns DEFAULT_MODELS[tier] (line 38). DO NOT MODIFY.

From src/__tests__/model-routing.test.ts (the test analog — replicate its style):
- Imports: `resolveModel, isAllowedModel, ALLOWED_MODELS, DEFAULT_MODELS` from "../index"
- Assertions reference the `DEFAULT_MODELS.fast` / `DEFAULT_MODELS.standard` CONSTANTS, never hardcoded model strings (e.g. `expect(model).toBe(DEFAULT_MODELS.standard)`).
- `createMockEnv({ kvData })` from "./helpers" seeds KV; default get returns null (empty KV).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types gate + Kimi-id decision + source edits (ALLOWED_MODELS, DEFAULT_MODELS)</name>
  <files>worker-configuration.d.ts, src/index.ts</files>
  <read_first>
    - .planning/phases/09-second-model-and-tier-split/09-CONTEXT.md (LOCKED decisions, esp. "Model choice" gated fallback)
    - src/index.ts lines 9-52 (the model block: ALLOWED_MODELS line 13, DEFAULT_MODELS line 23, resolveModel line 38)
    - worker-configuration.d.ts (the generated AiModels type — after regenerating)
  </read_first>
  <action>
    Run `npm run types` (which runs `wrangler types`) FIRST to regenerate
    worker-configuration.d.ts. Then inspect the regenerated `AiModels` type and decide
    the Kimi model id per the LOCKED gated fallback in 09-CONTEXT.md:
    - If `@cf/moonshotai/kimi-k2.7-code` is a key of `AiModels` → use it (preferred, coding-optimized).
    - Else → fall back to `@cf/moonshotai/kimi-k2.5` (confirmed a key in current generated types).
    Confirm the chosen id is present as a key of AiModels (grep the regenerated file for it).
    Record which id was chosen — it goes in the commit message at the end.

    Edit src/index.ts:13 — add the chosen Kimi id as a second element of the
    `ALLOWED_MODELS` array, keeping the `as const satisfies ReadonlyArray<keyof AiModels>`
    constraint intact (this is the compile-time proof the model is real). Do NOT remove
    `@cf/qwen/qwen3-30b-a3b-fp8`.

    Edit src/index.ts:23 — in `DEFAULT_MODELS`, leave `fast` as
    `@cf/qwen/qwen3-30b-a3b-fp8` and change `standard` to the chosen Kimi id. Keep the
    `Record<ModelTier, keyof AiModels>` annotation. Make NO other edits: do NOT touch
    `resolveModel`, `isAllowedModel`, `TASK_SPECS`, any handler, any prompt string, or
    the KV key strings.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    grep -q "moonshotai/kimi" src/index.ts && grep -E "moonshotai/kimi" worker-configuration.d.ts | grep -vc '^#'
  </verify>
  <acceptance_criteria>
    - `npm run types` completed; worker-configuration.d.ts regenerated and contains the chosen Kimi id as a key of AiModels.
    - src/index.ts ALLOWED_MODELS array contains BOTH `@cf/qwen/qwen3-30b-a3b-fp8` and the chosen Kimi id, still under `as const satisfies ReadonlyArray<keyof AiModels>`.
    - src/index.ts DEFAULT_MODELS.fast === `@cf/qwen/qwen3-30b-a3b-fp8` (unchanged) and DEFAULT_MODELS.standard === the chosen Kimi id.
    - No edits to resolveModel, isAllowedModel, TASK_SPECS, handlers, prompts, or KV key strings.
    - `npx tsc --noEmit` exits 0 (clean).
    - Chosen Kimi id recorded for the commit message.
  </acceptance_criteria>
  <done>tsc clean; standard tier now defaults to the chosen Kimi id; fast unchanged; allowlist + KV self-heal path structurally intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add model-routing test cases for the Kimi standard tier</name>
  <files>src/__tests__/model-routing.test.ts</files>
  <read_first>
    - src/__tests__/model-routing.test.ts (full file — replicate its constant-reference assertion style)
    - src/__tests__/helpers.ts (createMockEnv / createMockKV signatures)
    - .planning/phases/09-second-model-and-tier-split/09-CONTEXT.md ("Specific Ideas" section — the exact new cases)
  </read_first>
  <action>
    Add three new test cases to src/__tests__/model-routing.test.ts, matching the file's
    existing style (assert against the `DEFAULT_MODELS.standard` / `DEFAULT_MODELS.fast`
    CONSTANT references imported from "../index", NOT against hardcoded model strings —
    this keeps the test resilient to the gated id choice):
    1. In the `isAllowedModel` describe block: `isAllowedModel(DEFAULT_MODELS.standard)`
       returns `true` (the chosen Kimi id is allowlisted). You may assert the same against
       the literal chosen id if desired, but the constant-reference form is required at minimum.
    2. In the `resolveModel` describe block: with an empty-KV `createMockEnv()`,
       `resolveModel(env, "standard")` returns `DEFAULT_MODELS.standard`, and assert it is
       NOT equal to `DEFAULT_MODELS.fast` (proves the two tiers now diverge).
    3. In the `resolveModel` describe block: with an empty-KV `createMockEnv()`,
       `resolveModel(env, "fast")` still returns `DEFAULT_MODELS.fast`
       (=== `@cf/qwen/qwen3-30b-a3b-fp8`) — fast tier unchanged.
    Do NOT modify any existing assertion in this file, and do NOT touch
    runtask.test.ts or observability.test.ts (they assert tier NAMES, not models, and
    must survive untouched).
  </action>
  <verify>
    <automated>npm test -- src/__tests__/model-routing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - model-routing.test.ts contains the three new cases, all referencing DEFAULT_MODELS constants (no hardcoded model strings in assertions, matching existing style).
    - `npm test -- src/__tests__/model-routing.test.ts` exits 0 with the new cases passing.
    - No existing assertion in model-routing.test.ts was changed; runtask.test.ts and observability.test.ts are untouched.
    - Full suite `npm test` is green (existing 162 + new cases).
  </acceptance_criteria>
  <done>New cases prove standard→Kimi, fast→qwen3, Kimi allowlisted, and tiers diverge; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Update Model Tiers docs in CLAUDE.md and README.md</name>
  <files>CLAUDE.md, README.md</files>
  <read_first>
    - CLAUDE.md ("Model Tiers" table — the `## Model Tiers` section with the fast/standard rows)
    - README.md lines 30-46 (the Tools table Tier column + the "**Tiers:**" sentence at line 46; the architecture diagram line 15 "Workers AI (qwen3-30b)")
    - .planning/phases/09-second-model-and-tier-split/09-CONTEXT.md (scope fence — docs update is allowed and in-scope)
  </read_first>
  <action>
    Update the Model Tiers documentation to reflect the split (fast = qwen3-30b,
    standard = the chosen Kimi id from Task 1):
    - CLAUDE.md `## Model Tiers` table: change the `standard` row's Default cell from
      `@cf/qwen/qwen3-30b-a3b-fp8` to the chosen Kimi id. Leave the `fast` row as
      `@cf/qwen/qwen3-30b-a3b-fp8`. Keep the KV Key and Used By columns unchanged.
    - README.md line 46 "**Tiers:**" sentence: update so `standard` is described as the
      Kimi coding model and `fast` stays the lightweight qwen3 model; keep the
      "configurable via KV at runtime" clause. Optionally update the architecture diagram
      label at line 15 from "Workers AI (qwen3-30b)" to reflect two models, but only if it
      stays accurate. Do NOT change the per-tool Tier-column assignments (lines 32-44) —
      those tier NAMES are unchanged.
    Use the exact chosen Kimi id string recorded in Task 1. No other doc edits.
  </action>
  <verify>
    <automated>grep -c "moonshotai/kimi" CLAUDE.md && grep -ic "kimi" README.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md Model Tiers table: standard row Default = chosen Kimi id; fast row Default = @cf/qwen/qwen3-30b-a3b-fp8.
    - README.md "**Tiers:**" sentence describes standard as the Kimi coding model and fast as qwen3; tool-table Tier-column names unchanged.
    - The chosen Kimi id string in the docs matches exactly the id committed in src/index.ts.
  </acceptance_criteria>
  <done>Docs accurately describe the two-model split; tier names in tool tables unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KV → resolveModel | `config:model:*` KV values are operator-controlled config that flow into the model id passed to `env.AI.run`. Untrusted relative to the model allowlist. |

This phase adds NO new runtime input surface (no new tool params, no new handlers,
no prompt changes). The only security-relevant change is expanding the model allowlist.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | ALLOWED_MODELS / isAllowedModel (src/index.ts) | mitigate | Keep the `as const satisfies ReadonlyArray<keyof AiModels>` constraint — a non-existent/invalid model id fails the type-check, so the allowlist can only contain real models. Adding the Kimi id does not loosen `isAllowedModel`; it still gates KV overrides. |
| T-09-02 | Elevation of Privilege | resolveModel KV self-heal path | mitigate | Do NOT modify resolveModel — the self-heal (delete-invalid-and-fall-back) and the isAllowedModel gate on KV overrides stay intact, so an invalid `config:model:*` KV value still cannot inject an unlisted model. Test case (Task 2) plus the unchanged existing self-heal test prove the path survives. |
| T-09-03 | Tampering | npm/wrangler types regeneration | accept | `npm run types` runs `wrangler types` (already a project script) and only regenerates the local generated type file from the account's model catalog; no new package installs. No `[ASSUMED]`/`[SUS]` packages introduced — no install gate required. |

No new packages are installed in this phase (Kimi is a Workers AI catalog model, not
an npm dependency), so no Package Legitimacy Gate / install checkpoint applies.
</threat_model>

<verification>
Phase-level checks (run after all tasks):
- `npm run types` succeeded and worker-configuration.d.ts contains the chosen Kimi id as a key of AiModels.
- `npx tsc --noEmit` is clean.
- `npm test` is green: existing 162 tests + the 3 new model-routing cases; runtask.test.ts and observability.test.ts pass with no assertion changes (they assert tier names, not models).
- src/index.ts: ALLOWED_MODELS contains the Kimi id; DEFAULT_MODELS.standard = Kimi id; DEFAULT_MODELS.fast = @cf/qwen/qwen3-30b-a3b-fp8; resolveModel/isAllowedModel/TASK_SPECS unchanged.
- Docs (CLAUDE.md + README.md) reflect the split with the exact committed Kimi id.
</verification>

<success_criteria>
- MODEL-03 satisfied: `standard` tier resolves to a Kimi coding-optimized model; `fast` stays qwen3-30b; the Kimi id is in ALLOWED_MODELS with allowlist + KV self-healing preserved; tier names and every per-kind resolve() output unchanged.
- Behavior-shape preserved: no TASK_SPECS / prompt / handler edits; existing tier and observability assertions untouched and green.
- Build + full suite green: `npx tsc --noEmit` clean, `npm test` passing (162 existing + 3 new).
- Commit message records which Kimi id was chosen (`kimi-k2.7-code` preferred, `kimi-k2.5` fallback).
</success_criteria>

<output>
Create `.planning/phases/09-second-model-and-tier-split/09-01-SUMMARY.md` when done.
Record in the SUMMARY: which Kimi id was chosen and why (k2.7-code present vs k2.5 fallback),
and confirm the behavior-preservation invariant held (tier names + resolve outputs unchanged).
</output>

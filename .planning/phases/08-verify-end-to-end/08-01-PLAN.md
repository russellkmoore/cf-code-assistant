---
phase: 08-verify-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/batch-e2e.test.ts
autonomous: true
requirements: [BATCH-10]

must_haves:
  truths:
    - "(BATCH-10 / D-02 / D-03) A mixed 3-task batch (ok + validation-fail + deterministic timeout) driven through the REAL createMcpServer handler returns order-preserving partial results: results[i].index === i regardless of completion order"
    - "(BATCH-10 / D-03) All three error_type/status outcomes are proven in one batch: status:'ok', error_type:'validation', error_type:'timeout'"
    - "(BATCH-10 / D-03b) structuredContent is co-returned with content text and BatchOutputSchema.parse(structuredContent) does not throw; summary and failedIds reflect the two failures in input order"
    - "(BATCH-10 / D-04) An opt-in real-45s-wait e2e block exists as describe.skip — present, documented, and EXCLUDED from the default npm test run (asserts loosely: status:'error', no hang)"
    - "(BATCH-10 / D-01) Build gate is green: npm run types -> npx tsc --noEmit clean + full npm test green at 162 tests (161 existing single-task/unit tests still pass = behavior-preserving)"
  artifacts:
    - path: "src/__tests__/batch-e2e.test.ts"
      provides: "Committed fast 3-task mixed-batch e2e + describe.skip real-wait opt-in block"
      contains: "describe.skip"
      min_lines: 80
  key_links:
    - from: "src/__tests__/batch-e2e.test.ts"
      to: "createMcpServer + code_assist_batch handler"
      via: "(server as any)._registeredTools[\"code_assist_batch\"].handler"
      pattern: "_registeredTools\\[.code_assist_batch.\\]"
    - from: "src/__tests__/batch-e2e.test.ts"
      to: "BatchOutputSchema (src/index.ts)"
      via: "schema parse of structuredContent"
      pattern: "BatchOutputSchema\\.parse"
    - from: "src/__tests__/batch-e2e.test.ts"
      to: "readBatchConfig timeout path (src/batch.ts)"
      via: "BATCH_TASK_TIMEOUT_MS string override on mock env"
      pattern: "BATCH_TASK_TIMEOUT_MS"
---

<objective>
Prove the v2.0 `code_assist_batch` seam works end-to-end through the REAL `createMcpServer`
by adding ONE new test file — `src/__tests__/batch-e2e.test.ts`. The file contains (1) a
committed FAST 3-task mixed-batch e2e that runs in default `npm test` and proves
order-preserving partial results across all three status outcomes (ok / validation / timeout),
and (2) a `describe.skip` opt-in block that demonstrates the real 45s timeout race by hand.

This is a VERIFICATION phase. Per the locked CONTEXT.md decisions it MUST NOT modify
`src/index.ts`, `src/batch.ts`, the single-task tools, `package.json`, or `wrangler.toml`.
The ONLY deliverable is the new test file plus a clean build gate.

Purpose: Deliver BATCH-10 — demonstrate the registered batch tool returns correct
order-preserving partial results end-to-end while the single-task tools stay behavior-preserving
(their existing 161 tests stay green).
Output: `src/__tests__/batch-e2e.test.ts` (new); full suite green at 162 tests; `tsc --noEmit` clean.
</objective>

<execution_context>
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/workflows/execute-plan.md
@/Users/rmoore/.claude/plugins/cache/gsd-plugin/gsd/3.7.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verify-end-to-end/08-CONTEXT.md
@.planning/phases/08-verify-end-to-end/08-RESEARCH.md
@.planning/phases/08-verify-end-to-end/08-VALIDATION.md

# The analog to mirror (handler-invocation + env mock + BatchOutputSchema parse):
@src/__tests__/batch-tool.test.ts
# Existing engine-level coverage — do NOT duplicate it:
@src/__tests__/batch.test.ts
# Mock env / AI construction:
@src/__tests__/helpers.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from the repo this session — use directly, no exploration. -->

From src/index.ts (named exports, do NOT modify this file):
  export function createMcpServer(env: Env): McpServer   // registers code_assist_batch
  export const BatchOutputSchema                         // Zod schema; .parse(structuredContent)
  export function deriveErrorType(errMsg: string): "timeout" | "validation" | "ai_error"
  // deriveErrorType rules (substring, lowercased):
  //   includes "timeout" | "ai_timeout"      -> "timeout"
  //   includes "input_too_large" | "validationerror" -> "validation"
  //   else                                    -> "ai_error"
  // TASK_SPECS.transformCode.validate(): code > 8000 UTF-8 bytes -> throw ValidationError("INPUT_TOO_LARGE")
  // runBatch assigns id = t.id ?? String(index); failedIds preserves input order
  // timeout entry latency_ms = parseInt of /exceeded (\d+)ms timeout/  (= the timeout ms)

From src/batch.ts (do NOT modify this file):
  export { executeBatch, mapWithConcurrency, withTimeout, readBatchConfig }
  // withTimeout throws:  new Error(`Task exceeded ${ms}ms timeout`)
  // readBatchConfig(env) reads env.BATCH_TASK_TIMEOUT_MS (string) -> Number(); guard n>0 && finite
  //   defaults: BATCH_CONCURRENCY=6, BATCH_MAX_TASKS=50, BATCH_TASK_TIMEOUT_MS=45_000
  // runBatch casts env as Record<string,string|undefined>, so an extra string prop on the
  //   mock env object (BATCH_TASK_TIMEOUT_MS: "20") is picked up — it is NOT on the Env type.

From src/__tests__/helpers.ts:
  export function createMockEnv(overrides?): Env   // builds plain object: { OAUTH_KV, AI, MCP_SECRET, AUTH_RATE_LIMITER }
  // AI default mock: { run: vi.fn(async () => ({ response })) }

From src/__tests__/batch-tool.test.ts (the analog — copy its getToolHandler verbatim):
  function getToolHandler(env: Env, toolName: string)   // (server as any)._registeredTools[toolName].handler
  // Invocation: const result = await handler({ tasks: [...] });
  //   result.content[0]        -> { type:"text", text:string }   (the summary)
  //   result.structuredContent -> BatchOutputSchema-shaped object
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create batch-e2e.test.ts with the committed FAST 3-task mixed-batch e2e</name>
  <files>src/__tests__/batch-e2e.test.ts</files>
  <read_first>
    - src/__tests__/batch-tool.test.ts (copy getToolHandler verbatim incl. the SDK-internals warning comment; mirror the handler invocation + BatchOutputSchema.parse pattern)
    - src/__tests__/helpers.ts (createMockEnv signature; AI mock shape { run: vi.fn(async()=>({response})) })
    - src/index.ts (createMcpServer, BatchOutputSchema, deriveErrorType, TASK_SPECS.transformCode.validate 8000-byte cap, runBatch id/failedIds/latency_ms enrichment — READ-ONLY, do not modify)
    - src/batch.ts (withTimeout throw string `Task exceeded ${ms}ms timeout`; readBatchConfig BATCH_TASK_TIMEOUT_MS env read — READ-ONLY, do not modify)
    - .planning/phases/08-verify-end-to-end/08-RESEARCH.md (Patterns 1–6 + Code Examples: the exact env/AI-mock construction, the validation-fail recipe, the deterministic-timeout recipe, the order-preservation setup)
  </read_first>
  <behavior>
    - Order-preservation: with completion order != input order, structuredContent.results[i].index === i for i in {0,1,2}.
    - Timeout outcome (index 0): status:'error', error_type:'timeout', latency_ms === 20 (parsed from "Task exceeded 20ms timeout").
    - Validation outcome (index 1): status:'error', error_type:'validation' (transformCode >8000-byte input trips validate() before any AI call).
    - Ok outcome (index 2): status:'ok', result === the mocked AI text.
    - Co-return: result.content[0].type === 'text' AND result.structuredContent is defined; BatchOutputSchema.parse(structuredContent) does not throw.
    - Aggregates: total===3, succeeded===1, failed===2; failedIds === [id-of-index-0, id-of-index-1] in input order; summary reflects "1/3 ... succeeded" and "2 failed".
  </behavior>
  <action>
    Create the new file src/__tests__/batch-e2e.test.ts. Import { describe, it, expect, vi } from "vitest",
    and { createMcpServer, BatchOutputSchema } from "../index", { createMockEnv } from "./helpers".
    Copy the getToolHandler helper verbatim from batch-tool.test.ts (keep the SDK-internals warning comment
    so the `(server as any)._registeredTools["code_assist_batch"].handler` accessor is documented).

    Add a single describe block named for BATCH-10 (e.g. "BATCH-10: committed fast e2e — 3-task mixed batch
    through createMcpServer") with ONE it() test. Construct a mock env by spreading createMockEnv() and
    overriding two things: (a) AI.run as a fresh vi.fn() whose .mockImplementationOnce calls drive the two
    AI-reaching tasks — first call (the timeout task) returns a Promise that resolves AFTER ~100ms via
    setTimeout (so it is well past the timeout); second call (the ok task) resolves instantly with a known
    response string; (b) add the extra string property BATCH_TASK_TIMEOUT_MS: "20" so readBatchConfig picks
    up a 20ms per-task timeout (cast the whole object `as unknown as Env`; do NOT add BATCH_TASK_TIMEOUT_MS to
    the Env type or touch worker-configuration.d.ts).

    Per D-03a, order the 3-task `tasks` array so completion order != input order:
      index 0 = the TIMEOUT task (an AI-reaching kind, e.g. quickTask) — its AI mock hangs ~100ms so
                withTimeout(20ms) fires first;
      index 1 = the VALIDATION-FAIL task: kind "transformCode" with input.code being a string of >8000 UTF-8
                bytes (e.g. "x".repeat(8001)) plus an instruction — validate() throws ValidationError before
                any AI call (so this task never consumes an AI mock call);
      index 2 = the OK task (quickTask) — its instant AI mock makes it status:'ok'.
    Give the three tasks explicit `id`s (per RESEARCH Open Question 2, self-documenting failedIds), and assert
    failedIds equals the index-0 and index-1 ids in input order.

    Invoke the handler with { tasks } and assert the full behavior block above using the EXACT identifiers:
    structuredContent.results[i].index === i; results[0].status === "error" with results[0].error_type ===
    "timeout" and results[0].latency_ms === 20; results[1].error_type === "validation"; results[2].status ===
    "ok"; expect(() => BatchOutputSchema.parse(structuredContent)).not.toThrow(); total/succeeded/failed ===
    3/1/2; summary contains "1/3" and "2 failed". Pass an explicit 5000ms test timeout as the third it() arg.
    To harden against pool dispatch ordering (RESEARCH A1), also assert env.AI.run was called exactly twice
    (the validation-fail task never calls AI). Do NOT include any fenced implementation here — write the test
    against the identifiers named above.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/batch-e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/batch-e2e.test.ts exists and uses `_registeredTools["code_assist_batch"]` to obtain the handler
    - The test asserts `structuredContent.results[0].index === 0`, `results[1].index === 1`, `results[2].index === 2`
    - The test asserts `results[0].error_type === 'timeout'`, `results[1].error_type === 'validation'`, `results[2].status === 'ok'`
    - The test asserts `BatchOutputSchema.parse(result.structuredContent)` does not throw, and result.content[0].type === 'text'
    - The test asserts failedIds in input order and a summary containing "1/3" and "2 failed"
    - `npx vitest run src/__tests__/batch-e2e.test.ts` exits 0
    - src/index.ts, src/batch.ts, package.json, wrangler.toml are NOT modified (git diff shows only the new test file)
  </acceptance_criteria>
  <done>The committed fast e2e proves all three status/error_type outcomes in one order-preserving batch through the real createMcpServer, and runs green in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Add the describe.skip real-45s-wait opt-in block to the same file</name>
  <files>src/__tests__/batch-e2e.test.ts</files>
  <read_first>
    - src/__tests__/batch-e2e.test.ts (the file from Task 1 — append the opt-in block at the bottom)
    - .planning/phases/08-verify-end-to-end/08-RESEARCH.md (Pattern 7 + the "Opt-In 45s Real-Wait Test Pattern" code example + Pitfall 5 on the race; D-04a/D-04b reasoning)
    - src/index.ts (AI_TIMEOUT_MS = 45_000 and callModel's internal AbortController — READ-ONLY, context only)
  </read_first>
  <action>
    Append a second top-level block to src/__tests__/batch-e2e.test.ts using describe.skip (NOT it.todo)
    named for the opt-in real-wait race (e.g. "BATCH-10 opt-in: real-wait 45s timeout race (un-skip to run
    manually)"). Above the block, add a NOTE-TO-FUTURE-READER comment that (a) explains this exercises the
    real 45s wall-clock timeout, (b) states it uses an OFFLINE hanging AI mock — no real Workers AI, no
    charges, no network/credentials, (c) documents the race per D-04b: both withTimeout (BATCH_TASK_TIMEOUT_MS
    default 45s) and callModel's own internal AbortController (AI_TIMEOUT_MS = 45s) fire at the same instant,
    so which settles first is nondeterministic — therefore assert LOOSELY and do NOT assert error_type, and
    (d) gives the un-skip instruction.

    Inside, write one it() that builds a mock env spreading createMockEnv() with AI.run as a vi.fn returning a
    never-resolving Promise (`new Promise(() => {})`) — the hanging mock. Do NOT override BATCH_TASK_TIMEOUT_MS
    (use the real 45s default so the production race is faithful). Drive a small batch (e.g. one hanging task
    at index 0 plus one trivial task) through getToolHandler("code_assist_batch") and assert LOOSELY ONLY:
    results has the expected length, the hanging task's entry is present in input order, and its status ===
    'error', and the batch settled (no hang / no unhandled rejection). Do NOT assert error_type or the exact
    error message. Pass an explicit 60_000ms test timeout as the third it() arg (45s deadline + margin).

    Reuse the same getToolHandler helper already in the file. No fenced implementation here — follow the
    identifiers and the RESEARCH opt-in code example.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/batch-e2e.test.ts 2>&1 | grep -Eq "skipped|↓|·" && grep -q "describe.skip" src/__tests__/batch-e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/batch-e2e.test.ts contains `describe.skip` (grep -F 'describe.skip' matches)
    - The opt-in block uses a never-resolving AI mock (`new Promise(() => {})`) and an explicit 60_000 ms it() timeout
    - The opt-in block asserts ONLY status === 'error' + presence in input order (no error_type assertion); a comment documents the 45s race (D-04b)
    - Running `npx vitest run src/__tests__/batch-e2e.test.ts` reports the opt-in block as skipped and does NOT execute its 45s body (run completes in seconds, exits 0)
    - The committed fast test from Task 1 still passes in the same run
  </acceptance_criteria>
  <done>The opt-in real-45s-wait e2e exists as a skip-by-default block with loose assertions and a documented race; default runs skip it and stay sub-5s.</done>
</task>

<task type="auto">
  <name>Task 3: Build gate — types regenerate, tsc clean, full suite green at 162</name>
  <files></files>
  <read_first>
    - .planning/phases/08-verify-end-to-end/08-RESEARCH.md (Pitfall 3: worker-configuration.d.ts is gitignored — run `npm run types` BEFORE `npx tsc --noEmit`; D-01: no build script, no wrangler deploy)
    - .planning/phases/08-verify-end-to-end/08-CONTEXT.md (D-01 / D-01a: "clean build" = tsc --noEmit + npm test; no wrangler invocation)
  </read_first>
  <action>
    Run the build gate exactly as defined by D-01, in order: `npm run types` (regenerates the gitignored
    worker-configuration.d.ts — a generate step, NOT a source/package.json change, per D-01a), then
    `npx tsc --noEmit` (must be clean), then `npm test` (full suite must be green). Confirm the suite reports
    162 passing tests = 161 pre-existing + the 1 new committed fast e2e (the describe.skip block does NOT add
    to the passing count), and confirm the single-task tool tests (tool-handlers / observability /
    input-validation / runtask suites) still pass — this is the behavior-preservation evidence for SC#3. Do
    NOT add a build script, do NOT invoke wrangler deploy/dry-run, and make NO source changes in this task.
    If tsc reports a missing worker-configuration.d.ts type, that means `npm run types` was skipped — run it
    and retry.
  </action>
  <verify>
    <automated>npm run types && npx tsc --noEmit && npm test 2>&1 | grep -Eq "Tests[[:space:]]+162 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run types` completes and `npx tsc --noEmit` exits 0 (clean, no errors)
    - `npm test` exits 0 and reports `Tests  162 passed (162)` (161 existing + 1 new committed e2e)
    - The single-task tool suites (tool-handlers, observability, input-validation, runtask) are still green in that run
    - git status shows NO modifications to src/index.ts, src/batch.ts, package.json, or wrangler.toml (only the new src/__tests__/batch-e2e.test.ts is added)
  </acceptance_criteria>
  <done>The build gate is green: types regenerated, tsc --noEmit clean, full suite green at 162 with the single-task tools still passing (behavior-preserving), no source/package/wrangler changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase adds a test file under `src/__tests__/` only. The e2e drives an in-process handler obtained from `createMcpServer(env)` with a fabricated mock `env`; OAuth is intentionally out of path (already covered by auth-flow.test.ts, per D-02b). No new network endpoints, no new auth/trust boundaries, no new external input paths are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit = none). No install task exists; nothing to gate. |
| T-8-INFO | Informational | new test file src/__tests__/batch-e2e.test.ts | accept | Severity: none/informational. Test-only code, no production surface; the tool under test was security-reviewed in Phase 07 (T-7-01..T-7-04, all mitigated). The e2e bypasses OAuth by calling createMcpServer directly — correct integration-test architecture, not an auth regression. |
</threat_model>

<verification>
Phase-level checks (run after all three tasks):
- `npm run types && npx tsc --noEmit` — clean (D-01)
- `npm test` — green, `Tests  162 passed (162)`
- `npx vitest run src/__tests__/batch-e2e.test.ts` — committed fast e2e passes; opt-in block reported skipped
- `git status` — only `src/__tests__/batch-e2e.test.ts` added; no changes to src/index.ts, src/batch.ts, package.json, wrangler.toml
</verification>

<success_criteria>
- A mixed 3-task batch (ok + validation-fail + deterministic timeout) through the REAL createMcpServer returns order-preserving partial results: results[i].index === i, all three statuses/error_types correct (BATCH-10 / D-03)
- structuredContent + content text co-returned; BatchOutputSchema.parse(structuredContent) does not throw; summary/failedIds reflect the two failures in input order (D-03b)
- An opt-in real-45s-wait e2e exists as describe.skip with loose assertions and a documented race, excluded from default npm test (D-04)
- Build gate green: npm run types -> tsc --noEmit clean + npm test green at 162; single-task tools still pass = behavior-preserving (D-01, SC#3)
- No changes to src/index.ts, src/batch.ts, package.json, wrangler.toml, or the single-task tools
</success_criteria>

<output>
Create `.planning/phases/08-verify-end-to-end/08-01-SUMMARY.md` when done.
</output>
